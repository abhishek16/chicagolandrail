// After deploying the Worker, paste its URL here (no trailing slash).
// e.g. "https://metra-proxy.yourname.workers.dev"
export const API_BASE = "https://metra-proxy.resoluteacc.workers.dev";

// Optional privacy-first visitor analytics (Cloudflare Web Analytics — cookieless,
// no personal data). To enable: Cloudflare dashboard → Analytics & Logs → Web
// Analytics → Add a site (or enable it on the Pages project) → copy the token
// from the <script data-cf-beacon='{"token":"XXXX"}'> snippet and paste it here.
// Leave "" to disable. (You can instead just enable Web Analytics on the Pages
// project in the dashboard, which needs no code — in that case leave this "".)
export const CF_ANALYTICS_TOKEN = "";
